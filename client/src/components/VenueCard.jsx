export default function VenueCard({venue,onBook}){return <article className="venue-card"><div className="venue-image" style={{backgroundImage:`url('${venue.image}')`}}><span className="rating"><b>★</b> {venue.rating?.toFixed(1)}</span></div><div className="venue-body"><h3>{venue.name}</h3><p className="place">⌖ {venue.area}</p><div className="tags">{venue.sports.map(s=><span key={s}>{s}</span>)}</div><div className="card-bottom"><div className="price"><strong>₹{venue.pricePerHour}</strong> <small>/ hour</small></div><button className="slot-btn" onClick={()=>onBook(venue)}>View slots</button></div></div></article>}
